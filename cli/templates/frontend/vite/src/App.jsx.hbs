import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <div className="app">
      <header className="app-header">
        <h1>{{projectName}}</h1>
        <p>Bem-vindo ao seu projeto Gbit Framework</p>
      </header>
      
      <main className="app-main">
        <div className="card">
          <button onClick={() => setCount((count) => count + 1)}>
            Count is {count}
          </button>
        </div>
      </main>
      
      <footer className="app-footer">
        <p>Criado com Gbit Framework 🚀</p>
      </footer>
    </div>
  )
}

export default App
